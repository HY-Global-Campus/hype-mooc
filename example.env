# Copy to .env for local docker compose overrides.
GCAI_TOKEN=
GCAI_URL=
OIDC_CLIENT_ID=
OIDC_CLIENT_SECRET=
# Host port for the backend (default 8081 if 8080 is in use)
# BACKEND_PORT=8080
# Development-only: one-click login without MOOC OIDC (requires NODE_ENV=development)
# DEV_AUTH_BYPASS=true
# VITE_DEV_AUTH_BYPASS=true
