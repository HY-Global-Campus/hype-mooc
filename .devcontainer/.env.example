# Optional overrides for docker compose (copy to repo root as .env if needed)
GCAI_URL=
GCAI_TOKEN=
OIDC_CLIENT_ID=
OIDC_CLIENT_SECRET=
# Enabled by default in .devcontainer/docker-compose.yml
# DEV_AUTH_BYPASS=true
# VITE_DEV_AUTH_BYPASS=true
